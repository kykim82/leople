name = "leople-estimate"
compatibility_date = "2026-06-01"
pages_build_output_dir = "./public"

# D1 바인딩 — database_id 는 `wrangler d1 create leople-estimate` 실행 후 나온 값으로 교체
[[d1_databases]]
binding = "DB"
database_name = "leople-estimate"
database_id = "여기에_생성후_나온_UUID_붙여넣기"
